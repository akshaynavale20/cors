<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Embedded Content</title>
  </head>
  <body>
    <!-- Display the iframe if embedUrl is provided -->
    <% if (embedUrl) { %>
      <iframe
        src="<%= embedUrl %>"
        width="100%"
        height="100%"
        frameborder="0"
        allowfullscreen
      ></iframe>
    <% } %>
  </body>
</html>
